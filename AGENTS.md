# AGENTS.md

- Run `python3 -m unittest discover -s tests` before finishing code changes.
- Do not add API keys or paid news-service credentials to the repository.
- Keep the dashboard deployable as static GitHub Pages content under `docs/`.
- Use a temporary local server for previewing the dashboard; do not create a service.
